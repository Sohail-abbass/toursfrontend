import './globals.css';
import { Providers } from './store/Provider'; // adjust path if needed

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
